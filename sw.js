// MeatQuiz — service worker.
// v96: смена имени кэша форсирует переустановку оболочки на устройствах —
//   лечит рассинхронизацию, когда новый index.html (с режимом Find) уже
//   загружен, а MEAT_QUIZ.js ещё старый из кэша (без body_part) и Find писал
//   «Нет вопросов с отмеченной частью туши». Словарь всё равно тянется
//   stale-while-revalidate, но новый CACHE гарантирует свежую оболочку.
// v95: скорость загрузки фото + свежесть обновлений.
//   index.html: проба существования картинок переведена с Image() (скачивал
//   каждый файл ЦЕЛИКОМ, последовательно — «Загрузка фото…» висела секундами
//   на холодном открытии) на параллельные HEAD-запросы (только заголовки, тела
//   не качаются); при file:// автоматический фолбэк на Image(). Скачивается
//   только показанный кадр, остальные предзагружаются в фоне для свайпа.
//   sw.js: fetch с cache:'no-cache' для HTML и фоновой ревалидации — GitHub
//   Pages ставит max-age=600, и без этого свежая версия приезжала только по
//   Ctrl+F5 (браузер молча отдавал страницу из своего http-кэша до 10 минут).
// v94: ребрендинг CitizenShip → MeatQuiz. Новое имя кэша (старый кэш удалится в
//   activate), из SHELL убраны файлы civics-теста (Write/Read не имеют данных),
//   иконки перерисованы под буквы M Q, манифест/FAQ/логотипы обновлены.
//   Фотографии steak_images/ не прекэшируются (набор произвольный) — их
//   подхватывает stale-while-revalidate после первого показа.
// Стратегии: HTML — network-first; свои статические ресурсы —
//   stale-while-revalidate; чужие домены (Supabase, CDN) — мимо кэша.

const CACHE = 'meatquiz-v114';
// v114: честная синхронизация (репорт: «нихуя не заработало»).
//   Расследование: клиент v113 работал, SQL-патч был применён, но ПОСЛЕ него
//   не было ни одной попытки выгрузки (все upd на сервере = момент последнего
//   синка), а в диагностике висела ошибка от старой попытки. Хуже: в тот
//   момент кнопка «Сохранить в облако» рапортовала «☁️ успех», хотя сервер
//   отбросил свинину чек-констрейнтом — pushAllToCloud глотал ошибки.
//   Исправлено:
//   1) pushAllToCloud собирает ошибки всех 6 режимов и возвращает {ok,errors};
//   2) кнопка «Сохранить в облако» при отказе показывает КРАСНОЕ сообщение с
//      режимом/таблицей/причиной («Часть данных НЕ сохранилась…»), успех и
//      wm2_lastsync — только при реально чистой выгрузке;
//   3) cloudSyncNow тоже возвращает pushErrors;
//   4) устаревшая ошибка режима очищается после его успешного прохода —
//      диагностика больше не пугает следами починенных проблем.
// v113: свинина и время — полноправные граждане синка (полное ревью цепочки).
//   1) ВРЕМЯ: партии хронометрируются (ctOpen/ctRestart → ctFinish, кап 6ч),
//      копятся в timeMs локальной статистики каждого режима и уезжают на
//      сервер в civics_stats.total_time. Диагностика показывает время и
//      локально, и из облака.
//   2) СВИНИНА: статистика pork/pork_find теперь ВЫГРУЖАЕТСЯ (v100-запрет
//      снят); рейтинг питается ТОЛЬКО серверной civics_stats — серверный
//      триггер sync_leaderboard (SQL-патч) суммирует games/correct по 4
//      игровым режимам (test/find/pork/pork_find), write/read не считаются,
//      счёт монотонный (только вверх). Гейт «турнир выкл» применяется ко всем
//      4 режимам. czCloudPullStats принимает pork-строки с форсом meatSet.
//   3) ФАЙЛ ПРОГРЕССА: только веса и настройки. Статистика (в т.ч. Find и
//      свиная) больше не кладётся в экспорт и не принимается импортом —
//      блок-лист MQ_STATS_KEYS на оба направления (анти-чит: рейтинг файлом
//      не подделать; раньше fdstats/porkstats затирались файлом).
//   4) Возвращены патчи v112, потерянные в задеплоенном v17 (index был от
//      v111): трекинг wm2_bounduid + матрица guard/усыновление при регистрации.
//   Требует SQL: meatquiz_rating_v2.sql (чек-констрейнты mode + новый
//   sync_leaderboard + одноразовый пересчёт рейтинга).
// v111: авто-усыновление локального профиля при регистрации.
//   Если на устройстве есть локальный профиль и НЕТ живой облачной сессии,
//   кнопка «Регистрация» больше не открывает guard-модалку: аккаунт создаётся
//   автоматически, текущий профиль привязывается к нему (linkCloudToLocal),
//   весь локальный прогресс сразу выгружается на сервер (pushAllToCloud),
//   имя в рейтинге = имя профиля. Дубля не возникает: данные никогда не жили
//   под другим user_id. Со стартового экрана после регистрации — сразу вход
//   в приложение под этим же профилем (enter).
//   Guard-модалка осталась ТОЛЬКО для реально опасного случая: на устройстве
//   живая сессия ДРУГОГО аккаунта (его прогресс под новым user_id создал бы
//   дубль в рейтинге). Проверяется реальная сессия getSession, а не флаг
//   wm2_linked (он ставится при любом входе и после logout врёт).

// v110: защита от задавливания данных при синке с двух устройств + диагностика.
//   Двухдевайсный стенд подтвердил две дыры в старом czCloudPush:
//   (а) after-game push без pull регрессировал сервер (B выгрузил games=7,
//       следом A выгрузил свои 5 → на сервере 5, рейтинг проседал до
//       следующего синка B);
//   (б) при упавшем pull внутри cloudSyncNow (ошибки глотались) слепой push
//       затирал сервер насовсем (7→4).
//   Теперь push = read-merge-write: перед выгрузкой читаем серверные веса
//   (сливаем по seen, как pull) и серверную строку статистики (счётчики —
//   только вверх, max; last_pct/extra — от стороны, где игр больше). Если
//   сервер прочитать не удалось — выгрузка ПРОПУСКАЕТСЯ (ошибка видна в
//   диагностике), локальная копия цела до следующего успешного синка.
//   Диагностика (кнопка в настройках) теперь показывает и последнюю ошибку
//   ВЫГРУЗКИ (__czLastPushError: RLS/сеть/пропуск из-за guard), в обеих
//   ветках — с сессией и без.
// v109: ремонт регистрации и синхронизации (ревью кода).
//   1) requestRegister/regGuardExit получили catch: сетевой сбой в signUp больше
//      не unhandled rejection «кнопка мигнула и молчит» — показывается ошибка.
//   2) wipeLocalForRegister сносит ВЕСЬ прогресс (wm2_*weights, wm2_*stats,
//      syncbase, cuprank и пр.), а не только имя профиля: старые данные больше
//      не уезжают в civics_stats под новым user_id → дублей в рейтинге нет.
//      Настройки (звук/шрифт/набор) сохраняются.
//   3) Boot-pull ожил: стартовый czCloudPullAll был мёртвым (клиент Supabase ещё
//      не создан). Теперь pull выполняется в initSupabase после getSession, с
//      refreshHome() — прогресс с другого устройства виден сразу при открытии.
//   4) Режим find изолирован от meatSet (как test): czBuildSync/czCloudPullStats
//      форсируют beef — статистика Pork·Find больше не течёт в облако под
//      mode='find' (и не попадает в рейтинг), облачный Beef·Find не пишется в
//      pork-ключи.
//   5) regGuard: кнопка «Синхронизировать» видна только при живой сессии
//      (без неё она всегда упиралась в needAuth-тупик); тексты честные.
//   6) doRegister распознаёт анти-enumeration Supabase (identities:[]) как
//      «логин занят», а не «аккаунт создан».
//   7) Ошибки czCloudPush (RLS и пр.) пишутся в window.__czLastPushError и
//      видны в cloudDiagnostics (lastPushError).
//   8) leaderboard-insert не шлёт wk_start с клиента — считает default БД (UTC).
// v108: обе схемы (Beef/Pork · Find) построены на пустых силуэтах пользователя
//   (внешние файлы cow_diagram.svg 1982x1221, pig_diagram.svg 1663x1043).
//   Названия — настоящим ТЕКСТОМ шрифтом Nunito поверх силуэта. Подсветка
//   ТОЛЬКО текста: синий (выбор) → зелёный/красный (после проверки).
// v107: схема КОРОВЫ (Beef · Find) тоже вынесена во внешний файл
//   dictionary/cow_diagram.svg — из index.html убрано ~66 КБ встроенной
//   графики. Обе схемы теперь грузятся как внешние файлы + слой зон.
// v106: схема свиньи для Pork · Find подгружается из внешнего файла
//   dictionary/pig_diagram.svg (кладёт пользователь); в код рисунок не
//   встроен. Поверх — слой из 13 кликабельных зон в координатах 1047x709,
//   подсветка выбора/ответа заливкой зоны. Версия приложения 13.
// v105: подробная свиная схема (Pork · Find): два уха, пятачок, подгрудок,
//   копыта с раздвоением, хвост-завиток, круглый окорок + полный набор
//   внутренних линий отрубов. Подсветка — только текст. Контур будет
//   догладжен в следующих итерациях.
// v104: схема свиньи (Pork · Find) — оригинальный силуэт в мясницком
//   стиле, плавные кривые. Подсветка выбора/ответа теперь ТОЛЬКО на тексте
//   названия отруба (заливка фигуры не меняется). Коровья схема без изменений.
// v103: рефакторинг — удалён мёртвый легаси словарной эпохи: просмотрщик
//   грамматики #tviewer (разметка + 49 строк CSS), неиспользуемая модалка
//   #quitModal, две осиротевшие иконки поворота. Работоспособность всех
//   кнопок (настройки, оба ряда Test/Find, пост-игровой экран) проверена
//   прогоном полных партий через jsdom. Логика не менялась.
// v102: КРИТ. ФИКС — устранено падение скрипта на старте (meatSet исполь-
//   зовалась до объявления, temporal dead zone), из-за чего не навешивались
//   обработчики и НЕ РАБОТАЛА НИ ОДНА КНОПКА. Объявление meatSet поднято к
//   citSel/porkSel. Прогон через jsdom подтверждает: все onclick навешаны.
// v101: чистка легаси-названий (Citizen/CitizenShip → MeatQuiz в комментариях;
//   идентификаторы и серверные имена civics_* сохранены). Диагностика теперь
//   показывает все режимы (Beef Test/Find, Pork Test/Find) и читает данные
//   напрямую из ключей, не завися от активного набора. FAQ обновлён под два
//   набора мяса и синхронизацию свинины.
// v100: включена облачная синхронизация свинины — но ТОЛЬКО веса (прогресс/seen)
//   в civics_weights под mode 'pork'/'pork_find'. civics_stats свинины НЕ
//   выгружается, поэтому серверный триггер рейтинга (sync_leaderboard читает
//   civics_stats) свинину не видит: прогресс синхронизируется между
//   устройствами, рейтинг не затрагивается, серверную схему менять не нужно.
// v99: свинина стала отдельным вторым рядом (Beef / Pork), у Pork появились свои
//   Test и Find. Find свинины использует свиную схему туши (ctPigSVG) и свои
//   изолированные ключи прогресса (wm2_pork*). Поднятие версии кэша доставляет
//   новую оболочку на устройства.
// v98: добавлен режим Pork (свинина). Новый словарь dictionary/PORK_QUIZ.js в
//   SHELL; он тоже под network-first-веткой для dictionary/*.js. Фото свинины —
//   steak_images/pork/ (не прекэшируются, как и говяжьи). Поднятие версии кэша
//   гарантирует, что оболочка с кнопкой Pork доедет на все устройства.
// v97: раз и навсегда убираем «Файл вопросов устарел». Причина была в том, что
//   словари (dictionary/*.js) отдавались stale-while-revalidate: при онлайне
//   первым показывался СТАРЫЙ файл из кэша (без body_part), свежий подтягивался
//   только к следующему запуску. Теперь для dictionary/*.js стратегия
//   network-first: есть сеть — всегда свежий MEAT_QUIZ.js; кэш только офлайн-
//   резерв. Оболочка (шрифты/иконки) остаётся stale-while-revalidate.
const SHELL = [
  './',
  './index.html',
  './supabase.js',
  './manifest.webmanifest',
  './icon-192.png',
  './icon-512.png',
  './icon-512-maskable.png',
  './dictionary/MEAT_QUIZ.js',
  './dictionary/PORK_QUIZ.js',
  './dictionary/pig_diagram.svg',
  './dictionary/cow_diagram.svg',
  './fonts/nunito-cyrillic-400-normal.woff2',
  './fonts/nunito-latin-400-normal.woff2',
  './fonts/nunito-cyrillic-600-normal.woff2',
  './fonts/nunito-latin-600-normal.woff2',
  './fonts/nunito-cyrillic-700-normal.woff2',
  './fonts/nunito-latin-700-normal.woff2',
  './fonts/nunito-cyrillic-800-normal.woff2',
  './fonts/nunito-latin-800-normal.woff2',
  './fonts/nunito-cyrillic-900-normal.woff2',
  './fonts/nunito-latin-900-normal.woff2'
];

// Кэшируем оболочку по одному, чтобы отсутствие любого файла (напр. иконки)
// не ломало установку service worker.
self.addEventListener('install', e => {
  e.waitUntil(
    caches.open(CACHE).then(c =>
      Promise.all(SHELL.map(url => c.add(url).catch(() => {})))
    ).then(() => self.skipWaiting())
  );
});

self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', e => {
  const req = e.request;
  if (req.method !== 'GET') return;            // POST/PATCH (запись в Supabase) не трогаем

  const url = new URL(req.url);

  // ── ГЛАВНОЕ ИСПРАВЛЕНИЕ ──────────────────────────────────────────────
  // Любой запрос на ДРУГОЙ домен (Supabase REST/Auth, CDN с библиотекой и
  // шрифтами) уходит напрямую в сеть, минуя кэш воркера. Иначе ответ
  // рейтинга/статистики кэшировался «cache-first» и навсегда залипал на
  // старых данных, хотя на сервере уже свежие. Браузер сам сходит в сеть.
  if (url.origin !== self.location.origin) return;
  // ─────────────────────────────────────────────────────────────────────

  const isHTML = req.mode === 'navigate' ||
    (req.headers.get('accept') || '').includes('text/html');

  // HTML — «сначала сеть» (всегда свежая страница), офлайн — из кэша.
  if (isHTML) {
    e.respondWith(
      // cache:'no-cache' заставляет браузер сверить версию с сервером (ETag/304),
      // а не отдать HTML из своего http-кэша: GitHub Pages ставит max-age=600,
      // и без этого свежая страница приезжала только по Ctrl+F5.
      fetch(req, { cache: 'no-cache' }).then(res => {
        if (res && res.ok) {                       // не кэшируем ошибочные ответы (404/500) как оболочку
          const copy = res.clone();
          caches.open(CACHE).then(c => c.put('./index.html', copy)).catch(() => {});
        }
        return res;
      }).catch(() => caches.match(req).then(hit => hit || caches.match('./index.html')))
    );
    return;
  }

  // Словари вопросов (dictionary/*.js) — «сначала сеть». Это критично: данные
  // вопросов не должны залипать. При онлайне ВСЕГДА берём свежий файл (с
  // no-cache, чтобы обойти и http-кэш браузера), кэш — только офлайн-резерв.
  // Именно это окончательно лечит «Файл вопросов устарел»: пользователь больше
  // никогда не видит старый MEAT_QUIZ.js без body_part, пока есть сеть.
  if (/\/dictionary\/.*\.js(\?|$)/.test(url.pathname)) {
    e.respondWith(
      fetch(req, { cache: 'no-cache' }).then(res => {
        if (res && res.ok) {
          const copy = res.clone();
          caches.open(CACHE).then(c => c.put(req, copy)).catch(() => {});
        }
        return res;
      }).catch(() => caches.match(req).then(hit =>
        hit || new Response('', { status: 504, statusText: 'Offline' })
      ))
    );
    return;
  }

  // Остальные свои статические ресурсы (./fonts/*, иконки) —
  // «stale-while-revalidate»: мгновенно отдаём из кэша, а в фоне тянем
  // свежую версию в кэш. Так обновлённые наборы подхватываются при
  // следующем открытии без ручного поднятия версии кэша.
  e.respondWith(
    caches.match(req).then(hit => {
      // фоновая ревалидация тоже с no-cache: иначе «свежая» версия бралась бы
      // из http-кэша браузера (max-age=600 на GitHub Pages) и обновления
      // словаря/шрифтов задерживались на 10 минут
      const network = fetch(req, { cache: 'no-cache' }).then(res => {
        if (res && res.ok) {
          const copy = res.clone();
          caches.open(CACHE).then(c => c.put(req, copy)).catch(() => {});
        }
        return res;
      }).catch(() =>
        // Офлайн и ресурса нет в кэше. ВАЖНО: нельзя отдавать index.html на
        // НЕ-HTML запрос (.js/шрифт/иконка) — иначе HTML попадёт в контекст
        // <script>/шрифта и сломает разбор. Навигации обрабатываются выше,
        // здесь только под-ресурсы → отдаём честный 504.
        hit || new Response('', { status: 504, statusText: 'Offline' })
      );
      return hit || network;
    })
  );
});
